{
  "name": "tridichess",
  "type": "module",
  "scripts": {
    "dev":           "npx http-server . -p 8080 -c-1",
    "test":          "vitest run",
    "test:watch":    "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0"
  }
}
